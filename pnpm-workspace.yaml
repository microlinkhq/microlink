lockfile: false

packages:
  - 'packages/*'

# 10.4+ throws when lerna-lite compiles the preset through Handlebars.
overrides:
  conventional-changelog-conventionalcommits: 10.3.0

allowBuilds:
  simple-git-hooks: true
  spawn-sync: false
